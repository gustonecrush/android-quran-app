<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.AndroidQuranApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary1</item>
        <item name="colorPrimaryVariant">@color/primary2</item>
        <item name="colorOnPrimary">@color/text</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/primary2</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="bottomNavigationView">
        <item name="fontFamily">@font/poppins_regular</item>
    </style>

    <style name="TabLayoutStyle" parent="Widget.Design.TabLayout">
        <item name="tabIndicator">@null</item>
        <item name="tabTextColor">@color/primary2</item>
        <item name="textAllCaps">false</item>
        <item name="fontFamily">@font/poppins_semibold</item>
    </style>

    <style name="CustomProgressBar" parent="android:Widget.ProgressBar">
        <item name="colorAccent">@color/primary2</item>
    </style>
</resources>